kind = "web"
previewPath = "/"
title = "Vibe App"
version = "1.0.0"
id = "artifacts/vibe-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24678

[services.development]
run = "pnpm --filter @workspace/vibe-app run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/vibe-app", "run", "build" ]
publicDir = "artifacts/vibe-app/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24678"
BASE_PATH = "/"
